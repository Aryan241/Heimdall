# DepthWizard / Heimdall
# Core dependencies — platform-agnostic.
# See INSTALL_PYTORCH.md for platform-specific PyTorch install instructions.

# ---------- Core ML ----------
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
Pillow>=9.5

# ---------- Depth Anything V2 ----------
transformers>=4.44,<4.46
safetensors>=0.4
timm>=0.9

# ---------- Geospatial ----------
rasterio>=1.3
shapely>=2.0

# ---------- CLI / config ----------
tqdm>=4.65

# ---------- Mesh generation ----------
trimesh>=4.0
open3d>=0.17; sys_platform != 'darwin' or platform_machine != 'arm64'
# Note: open3d may have issues on Apple Silicon — fallback is trimesh-only pipeline.

# ---------- Evaluation ----------
pandas>=2.0
tabulate>=0.9

# ---------- Optional: visualization / debugging ----------
matplotlib>=3.7
